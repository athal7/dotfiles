# Allow favicon (overrides global gitignore)
!favicon.svg

# Allow repo-level opencode config
!.opencode/opencode.json

# Allow .sql files (skill query scripts)
!*.sql

# gws CLI download artifacts
download.html
