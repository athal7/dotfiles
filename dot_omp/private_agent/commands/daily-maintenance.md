---
description: Run daily production-error triage and knowledge enrichment
---

Run both daily maintenance workflows in this session.

1. Read `~/.omp/agent/commands/fix-prod-errors.md` and complete that workflow first.
2. Read `~/.omp/agent/commands/kb-enrich.md` and complete that workflow second.
3. Keep the workflows independent. Report each result separately. A failure in one workflow must not prevent the other workflow from running unless the session cannot continue safely.
