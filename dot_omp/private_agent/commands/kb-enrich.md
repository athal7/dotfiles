Enrich KB routing state for every date since the last run. `backfill` requests one-time all-scope projection through the `knowledge-base` skill.

1. Resolve the normal enrichment range from `kb journal list`: enrich from the last recorded date plus one through today; with no journal entries, use today. With `backfill`, do not derive a journal window.
2. Read `chezmoi data --format json | jq -r '.kb.collectors[]'`. Run only configured recipes under `~/.config/kb/collectors/`. Treat Confluence, Slack, Linear, GitHub, OMP, OpenSpec, and explicitly approved local private sources as collector inputs. Stop if the collector list is absent.
3. Run configured collectors for the range. Preserve source identity, source type, version or modification time, URL or stable key, normalized content fingerprint, observed time, and collector name. Treat OMP session JSONL files as the coding-session source. Use each session's plan and task state only as extraction context.
4. Reconcile `~/.local/share/kb/apm-fix-ledger.jsonl` by worktree. Do not re-extract `filed`, `declined`, `noise-confirmed`, or `send_failed` sessions. Surface pending fix drafts as action-item candidates and record self-classified noise as `noise-confirmed`.
5. Classify extracted material as canonical fact, publication candidate, action item, temporary status, or deliberate skip. Reconcile canonical facts and source evidence in KB. Preserve each record's access classification. Deduplicate by normalized content and source identity. Account for every item with a disposition.
6. Do not exclude a Confluence Decision Log page by label. Skip it only when its page identity and normalized content fingerprint exactly match a KB write-back entry. A missing marker, missing entry, or changed content is independently authored source material and remains eligible.
7. Load `knowledge-base` for projection. It performs the upstream capability gate and produces the normal or backfill plan. Show the complete plan manifest and every source/access classification for explicit approval. If the capability gate fails, make no projection call and retain KB fallback.
8. After approval, let `knowledge-base` run the upstream approved projection and verification flow. Do not create or mutate projection state, approval digests, recovery records, replacement records, completion markers, or verification results in this command.
9. A Confluence publication is separate and approval-gated. Show the exact target, complete body, and source identity. Link a CQ KU only when it exists. On approval, record publication provenance through the existing KB publication path. Do not make a remote write without approval.
10. Write journal sections with `kb journal append`, merge supported profile facts, and deduplicate action items against current destination state and their underlying source. Create local reminders directly. Before any remote issue, project, message, or document write, show every destination and complete proposed content for approval.
