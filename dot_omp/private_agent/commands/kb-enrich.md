Enrich KB routing state for every date since the last run. `backfill` requests one-time all-scope projection through the `knowledge-base` skill.
Run KB enrichment in the caller session using the installed `kb` CLI against local runtime state. Do not create a session in, change directory to, or delegate this workflow to the kb tooling source repository.

1. Resolve the local IANA timezone before calculating any implicit date: `readlink /etc/localtime | sed 's#.*/zoneinfo/##'` (for example, `America/Chicago`). Local journal labels remain `YYYY-MM-DD`: for an explicit journal range, use the supplied labels unchanged. Otherwise resolve the normal enrichment range from `kb journal list`: calculate `today` as the calendar date in that IANA timezone, never UTC; enrich from the last recorded date plus one through that local `today`; with no journal entries, use that local `today`. For collectors, convert the inclusive local-label range to a half-open UTC instant window from local midnight at the range start through local midnight after the inclusive range end. With `backfill`, do not derive a journal window.
2. Read `chezmoi data --format json | jq -r '.kb.collectors[]'`. Run only configured recipes under `~/.config/kb/collectors/`. Treat Confluence, Slack, Linear, GitHub, OMP, and explicitly approved local private sources as collector inputs. Stop if the collector list is absent.
3. Run configured collectors for the range. Preserve source identity, source type, version or modification time, URL or stable key, normalized content fingerprint, observed time, and collector name. For every configured collector, emit exactly one report record with this shape:

   ```yaml
   collector: <configured collector name>
   terminal_status: succeeded | succeeded with no eligible evidence | failed
   coverage:
     state: exhaustive | non-exhaustive | not-applicable
     reasons: [<concrete reason>]
   counts:
     discovered: <integer>
     read: <integer>
     eligible_evidence: <integer>
     known_omitted: <integer>
     out_of_allowlist: <integer>
   ```

   Always emit every count, including zero. `terminal_status` reports whether the collector ran; it never implies coverage. `coverage.state` reports the scope that was actually read. Set it to `non-exhaustive` and include `pagination unavailable` whenever a paginated source cannot page through requested scope. A bounded source must also report `non-exhaustive` with its declared bound. Use `known_omitted` only for omissions that the collector can count; do not invent a count for an unknown unread remainder. Treat OMP session JSONL files as the coding-session source. Use each session's plan and task state only as extraction context; retain every eligible session in aggregate journal counts without a separate durable-plan lookup. For the configured `zoom` collector, enumerate every accessible Google Calendar with `list_calendars`, use `list_events` for each calendar, and call `get_event` for every candidate. Call Zoom `get_meeting_assets` only for resolved events with `conference.conference_id`. Dedupe calendar mirrors only when their resolved occurrence identity (`iCalUID` plus occurrence/start identity) and conference ID match. Never merge distinct recurring occurrences solely because they share an `iCalUID` or conference ID. Retain a Zoom asset only when its `meeting_id` matches the resolved Calendar `conference.conference_id` and its `meeting_start_time` exactly matches the resolved Calendar occurrence start. Preserve Calendar notes and attachments as separate Calendar evidence from Zoom summaries, My Notes, and transcripts.
4. Reconcile `~/.local/share/kb/apm-fix-ledger.jsonl` by worktree. Do not re-extract `filed`, `declined`, `noise-confirmed`, or `send_failed` sessions. Surface pending fix drafts as action-item candidates and record self-classified noise as `noise-confirmed`.
5. Synthesize related facts across all collectors into durable-workstream candidates before filing. Require a named outcome, internal ownership, and enough evidence for Who, What, Why, and measurable goals. Reject routine status, one bounded fix, and other-owned work. Reconcile each candidate independently against local KB projects, the applicable tracker projects, and existing issues; record an existing-project update, missing-project proposal, issue-only disposition, temporary status, or deliberate skip. A match in one destination never substitutes for checking the others.
6. Promote durable decisions instead of leaving them only as journal bullets. Treat changes to system boundaries, data ownership or flow, security or access, compatibility, or standing implementation constraints as durable. For every finalized fact, consider independent destinations: canonical KB, suitable Confluence publication, and a CQ candidate only when it is durable, reusable operational guidance. Canonical KB reconciliation is mandatory. Confluence and CQ are optional and each needs its own suitability decision and disposition; a fact may go to any subset. Preserve each record's access classification. Deduplicate by normalized content and source identity.
7. Do not exclude a Confluence Decision Log page by label. Skip it only when its page identity and normalized content fingerprint exactly match a KB write-back entry. A missing marker, missing entry, or changed content is independently authored source material and remains eligible.
8. Load `knowledge-base` for projection. It performs the upstream capability gate and produces the normal or backfill plan. Show the complete plan manifest and every source/access classification before projection. If the capability gate fails, make no projection call and retain KB fallback.
9. After the plan is complete, let `knowledge-base` run the upstream projection and verification flow. Do not create or mutate projection state, authorization digests, recovery records, replacement records, completion markers, or verification results in this command.
10. A Confluence publication is separate. Show the exact target, complete body, and source identity. Link a CQ KU only when it exists. Record publication provenance through the existing KB publication path.
11. Write journal sections with `kb journal append`, merge supported profile facts, and record decisions supported by OMP session evidence and configured collectors. Deduplicate action items against both current destination state and their underlying source before filing.
12. Create local reminders directly. Before any remote issue, project, message, document, Confluence, or CQ write, show every destination and complete proposed content. Preserve the collector privacy exclusions.
13. Account for every extracted fact, durable-workstream candidate, and durable decision with a disposition for each considered destination.
