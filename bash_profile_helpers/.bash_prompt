export PS1="$BLUE\W$YELLOW \$(git_branch)$PINK \$(rvm_version)\n$GREEN$ $OFF"

function rvm_version {
  echo "•"
}

function git_branch {
  echo "$(__git_ps1 "%s")"
}

# show directory in iterm header
export PROMPT_COMMAND='echo -ne "\033]0;${PWD/#$HOME/~}\007"'
