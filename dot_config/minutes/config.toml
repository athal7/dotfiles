output_dir = "/Users/athal/meetings"

[identity]
name = "Andrew Thal"

[transcription]
model = "small"
model_path = "/Users/athal/.minutes/models"
min_words = 3

[diarization]
engine = "none"

[summarization]
engine = "agent"
agent_command = "claude"  # uses Claude Code subscription, no API key needed

[search]
engine = "builtin"

[security]
allowed_audio_dirs = []
