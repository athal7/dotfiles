---
description: Reclaim disk space — stale worktrees, databases, OpenCode session entries
subtask: true
---

Use your `cleanup` capability to reclaim disk space and remove stale artifacts.

If a scope is provided, focus on that area:

$ARGUMENTS
