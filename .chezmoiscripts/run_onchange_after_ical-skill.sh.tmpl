#!/bin/sh
# Install ical agent skill when the ical binary changes version
# Hash: {{ output "ical" "version" | sha256sum }}

if ! command -v ical >/dev/null 2>&1; then
  echo "ical not found — skipping skill install" >&2
  exit 0
fi

ical skills install --agent others
