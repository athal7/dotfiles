#!/bin/sh
{{ template "brewenv" . }}

# Start ocdc polling service if not already running
# Check for any active state (started, scheduled, etc.) not just "started"
if ! brew services list | grep -q "^ocdc"; then
  echo "Starting ocdc polling service..."
  brew services start ocdc
else
  echo "ocdc polling service already running"
fi
