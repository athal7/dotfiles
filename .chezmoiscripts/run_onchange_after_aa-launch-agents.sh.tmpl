#!/bin/sh
# LaunchAgent generator — delegates to launchd-yaml.
# Re-runs whenever agents.yaml changes.
#
# agents.yaml hash: {{ include "dot_config/launchd-yaml/agents.yaml" | sha256sum }}

set -eu
launchd-yaml apply
