# Keep gh authentication tokens local (don't sync across machines)
.config/gh/hosts.yml

# Keep poll configs private (contains repo paths and personal workflow)
.config/opencode-pilot/

# Machine-specific attention sources (Linear team IDs, local tools, etc.)
.config/opencode/attention-sources.local

# Local command overrides (contain personal details like locations, team IDs)
.config/opencode/command.local/

# opencode.json is host-specific (model config, MCP servers)
# Using modify_ script to merge chezmoi-managed settings with local config
# .config/opencode/opencode.json

# Repo documentation - not for home directory
AGENTS.md
README.md
