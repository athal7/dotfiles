---
name: meetings
description: Meeting recording and processing via minutes CLI
license: MIT
metadata:
  provides:
    - meetings
---

`minutes` CLI — call directly via Bash; read `minutes --help` on demand.
