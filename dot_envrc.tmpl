# Auto-generated by chezmoi from dot_envrc.tmpl — do not edit directly.
# Exports secrets from macOS Keychain (service "chezmoi") as environment variables.
# Direnv caches the result until this file changes.

_envrc_secret() {
  local val
  val=$(security find-generic-password -s "chezmoi" -a "$1" -w 2>/dev/null) && export "$2=$val"
}

{{ range $name, $cfg := .secrets -}}
_envrc_secret {{ $name }} {{ $name | upper }}
{{ end -}}

unset -f _envrc_secret
